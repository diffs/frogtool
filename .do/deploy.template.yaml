spec:
  name: frogtool
  services:
    - build_command: yarn build
      environment_slug: node-js
      git:
        branch: main
        repo_clone_url: https://github.com/DanWritesCode/frogtool.git
      name: frogtool